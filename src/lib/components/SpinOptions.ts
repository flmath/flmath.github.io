export class SpinOptions {
	duration: number;
	delay: number;
	rotations: number;
  
	constructor(duration : number, delay : number, rotations : number = 0)
	{
	this.duration = duration;
	this.delay = delay;
	this.rotations = rotations;
  };
  }
  